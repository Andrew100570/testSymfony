
{% block body %}
    <style>
        .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    </style>



        <li>Номер заявки:{{ application.id }}</li>
        <li>Описание:{{ application.discription }}</li>
        <li>{{ application.timeofcreation |date('d-m-Y')}}</li>
        <li>{{ application.lstedittime |date('d-m-Y')}}</li>
        <li>Данные пользователя:{{ application.username }}</li>
        <li>Cтатус:{{ application.status }}</li>
        <li>Комментарий:{{ application.comments }}</li>
        <br>
    <h1>Comment</h1>

    {{ form_start(commentForm) }}

    {{ form_row(commentForm.discription) }}
    {{ form_row(commentForm.username) }}
    {{ form_row(commentForm.number_application) }}


    <button type="submit" class="btn">Add comment</button>
    {{ form_end(commentForm) }}


    {% for comment in comments %}
        <li>Номер комментария:{{ comment.id }}</li>
        <li>Описание:{{ comment.discription }}</li>
        <li>Время создания{{ comment.dateofcreation |date('d-m-Y')}}</li>
        <li>Пользовател,который комментировал:{{ comment.numberapplication }}</li>

        <br>

    {% endfor %}

{% endblock %}
